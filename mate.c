#include <stdio.h>
#include <math.h>

int main() {
    double x = 2.0;
    double y = 3.0;

    double result1 = pow(x, y); // x elevado a la potencia de y
    double result2 = sqrt(x);   // raíz cuadrada de x

    printf("Resultado 1: %f\n", result1);
    printf("Resultado 2: %f\n", result2);

    return 0;
}
